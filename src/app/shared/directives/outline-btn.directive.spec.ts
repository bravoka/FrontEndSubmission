import { OutlineBtnDirective } from './outline-btn.directive';

describe('OutlineBtnDirective', () => {
  it('should create an instance', () => {
    const directive = new OutlineBtnDirective();
    expect(directive).toBeTruthy();
  });
});
